# Core dependencies (always required)
docker>=7.0.0
pyyaml>=6.0.1
requests>=2.31.0
rich>=13.7.0
python-dotenv>=1.0.0
neo4j>=5.15.0
fastapi>=0.110.0
uvicorn>=0.29.0
jinja2>=3.1.3

# VectorRAG — offline semantic memory (install once, runs forever without internet)
# These are OPTIONAL: the system runs fine without them but RAG will be disabled.
# Install with:  pip install sentence-transformers chromadb
sentence-transformers>=2.7.0
chromadb>=0.5.0

# Backend API — FastAPI, authentication, WebSocket
fastapi-cors>=0.0.6
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt>=3.2.2,<4.0
email-validator>=2.1.0
websockets>=12.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0
httpx>=0.25.0
